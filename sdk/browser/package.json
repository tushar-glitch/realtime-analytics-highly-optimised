{
  "name": "@analytics/browser-sdk",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/tracker.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --outfile=dist/tracker.min.js && esbuild src/index.ts --bundle --outfile=dist/tracker.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "typescript": "*"
  }
}
